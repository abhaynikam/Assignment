var canvasLib = {
	canvasID : "",
	canvasName : "",
	createCanvas : function () {
		console.log("called");
		can = document.createElement("canvas");
		can.setAttribute("name",this.canvasID);
		can.setAttribute("id",this.canvasName);
		document.body.appendChild(can);
	}
	
				
}